# arise-iva dependencies
# Install with: pip install -r requirements.txt

# Core computer vision
opencv-python>=4.8.0

# Hand landmark detection
# NOTE: Do NOT install tensorflow alongside mediapipe — protobuf conflict.
mediapipe==0.10.9

# Protobuf — required version for mediapipe 0.10+ on Python 3.11
protobuf>=4.21.0,<5.0.0

# Numerical computing
numpy>=1.24.0

# ML model training and inference
scikit-learn>=1.3.0
pandas>=2.0.0

# Offline text-to-speech (no internet required)
pyttsx3>=2.90